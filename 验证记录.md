# 本次验证记录

## 已执行并通过

- JavaScript 语法：`node --check dist/app.js` 与 `node --check dist/config.js`。
- `node tests/smoke.cjs`：12 个断言，覆盖中文／英文／大小写拼音搜索、姓氏排序、字母与院系组合筛选、重音归一化和 HTML 转义。所有断言通过。
- 浏览器演示：初始六条虚构记录按排序名排列；输入 zhang 显示张明远；输入 Wilson 显示 James Wilson；输入无匹配词显示空状态。
- 模板选择：三种模板均展示，通用模板可进入完整录入表单。
- 新建保存：创建“测试教授”、排序名 Test Professor、测试学院，新增“办公室 / Office”字段并保存 A-301；目录总数从六变七，重新打开正确读回 A-301。
- 刷新演示：临时新增记录消失，恢复六条虚构数据，与页面说明一致。
- 页面工具：search_faculty_directory 注册成功；未登录调用明确拒绝；演示状态输入 zhang 返回 count=1 并更新页面；数字类型 query 明确拒绝。
- 桌面 1440×1000：实际截图检查目录、侧栏、筛选和表格，未见重叠。
- 手机 390×844：实际截图检查搜索、筛选、字母换行及空状态。页面无额外横向溢出；宽表格在独立容器内可横向滚动。
- 本机 HTTP 服务正常打开登录与演示。HTML 引用的 app.js、config.js、style.css 均存在。

## 尚未执行

用户确认目前没有 Supabase 项目，先交付可部署网站和接入说明。因此没有声称下列检查通过：

- 在真实 Supabase 执行 setup.sql，验证密码登录与令牌刷新。
- 未登录、非成员、只读成员、编辑成员四类数据库／存储权限检查。
- 真实附件上传下载、文件大小／类型的服务端拒绝行为。
- 两个真实账号同时编辑冲突、跨浏览器同步。
- GitHub Actions 云端构建和 Pages 发布链接。

上述代码及配置已交付，正式使用前按 README 完成接入及远端验收。
