# Faculty Atlas — 协作约定

## 目标与完成标准
维护供学校 HR 使用的教授电子档案。GitHub Pages 托管静态界面，Supabase 负责个人账号、共享档案及私有附件。最终以用户当前请求的可用行为为准。

## 执行方式
- 沿用已确认目标和授权，自行完成范围内可逆实现，不将计划、预览或阶段总结变成批准关卡。
- 只在缺失信息实质影响方案或操作目标时集中提问；继续不依赖答案的工作。等待不等于授权。
- 以问题规模决定读取文件、使用工具、测试及拆分代码的范围；不设固定步骤、工具次数或子代理数量。
- 先诊断失败再修复。遇到外部写入结果不明，先核实状态再重试。
- 依赖账号或权限的部分受阻时，完成其余部分，并准确报告仍缺少的配置；演示通过不等于生产接通。

## 项目约定
- dist/app.js：模板、交互、Supabase REST 访问；dist/style.css：响应式界面；database/setup.sql：权限与数据约束。
- 中文及英文原名原样保存；sortName 单独存拼音／拉丁字母排序名，多音字由 HR 校正。
- 扩展档案内容优先通过字段描述与 custom 数组完成；仅在新增业务需要时引入架构或依赖。
- 数据库按一个学校团队隔离。viewer 只读，editor 可新增和修改。只有 Supabase 管理员维护成员名单。
- 不将教授真实资料、密码、令牌、service_role 或 secret key 写入 Git 仓库。前端仅配置项目 URL 与 publishable/anon 公钥。
- 权限必须由数据库／存储服务检查。保留版本冲突检测，不能用前端隐藏按钮代替权限控制。
- 演示数据明确标注虚构并与正式档案分开；不编造真实档案、采访、研究成果或审核结论。

## 验证与交付
- 按改动验证受影响行为：搜索、模板、保存、附件、权限或并发中的相关部分。通过后不无依据重复测试。
- 本机预览：python3 -m http.server 4173 --bind 127.0.0.1 --directory dist。
- 语法检查：node --check dist/app.js；node --check dist/config.js。
- 正式接通前执行 README 的账号权限及双账号冲突检查。缺少后端时明确标为未执行。
- 超出已有授权的发布、发信、付费、权限变更、不可逆删除遵守适用审批规则；其他工作继续推进。
- 交付说明已完成行为、产物位置、实际验证和必要限制。不承诺尚未运行的服务。
