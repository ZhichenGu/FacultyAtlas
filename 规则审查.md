# GPT-6 Astra 协作规则审查

## 结论

本次将优化落实为项目内精简的 AGENTS.md、直接可读的字段描述、明确的数据权限与验收边界。没有更改用户全局规则、已安装 Skills 或系统限制，也没有添加未经请求的模型调用、API 费用或自动上传 HR 数据。

官方 GPT-6 Astra 指引强调沿用已隐含的授权、完成可自主执行的准备，并按任务规模校准测试；也指出模糊或冲突的 Skill 指令可能导致提前暂停。以下是对本次提供的规则与所读网站构建指引的具体适用性分析。

## 可能产生阻碍的规则

| 来源与原文 | 潜在影响 | 本项目处理 |
|---|---|---|
| 用户网站需求：“所有的优化”这一任务表述（非一条具体规则） | 无边界地扩展功能或反复打磨，无法判断完成 | 以原需求七项及确认的多人共享为范围；增加搜索、模板、字段、登录、权限和冲突检测，不自动扩展为薪酬／绩效系统 |
| 用户 AGENTS：“Skills 只加载与当前交付直接相关的部分” | 如果解释为可以跳过工具的强制前置，可能导致工具失败 | 只读必要内容，同时遵守更高优先级的工具前置 |
| 用户 AGENTS：艺术作品集默认 Figma 1920×1080 | 若只因“UI”关键词套用，会引入不必要 Figma 工作和固定画布 | 该段不适用于 HR 网站；采用响应式网页 |
| 网站构建 Skill：“Build the complete requested site, then use sites-hosting to publish it” | 机械应用会把用户要求的 GitHub 托管改成另一托管商 | 同一 Skill 明确要求尊重其他托管选择；交付 GitHub Pages 工作流，不注册或发布 Sites |
| 网站构建 Skill：“The first meaningful preview gate is the only local opening point” | 若误读成用户必须批准预览，容易提前停止 | 预览是检验与展示步骤，不是新批准关卡；打开后继续实现和验证 |
| 网站构建 Skill：变量、组件、图片生成及多项能力指引 | 不加判断全部实施可能过度工程 | 本站是表格工作台，不生成装饰图片、不引入完整组件库，使用原生静态页面与 Supabase REST |
| 用户 AGENTS：“以用户要求的产物和验收条件为完成标准” | 本身合理；若在缺账号时假称上线，会掩盖未完成工作 | 区分可运行演示、后端代码完成、正式环境验证、发布四个状态 |
| 用户 AGENTS：“依据适用规则取得明确授权” | 若忘记已确认的授权，会每一步重复提问 | 沿用既有授权；只针对新目的地、范围或风险变化判断，不为可逆实现重复确认 |

## 建议保留

用户提供的协作偏好整体已较适合自主执行，尤其是沿用授权、最少澄清、失败后诊断、验证与风险相称、局部阻碍不终止独立工作。不需要通过删除这些规则获得“更强”的模型表现。

仅凭本任务不能证明某条规则必然使模型失败，上表说明的是误用风险及本项目的处理。账号不存在、沙箱拒绝和外部权限不足属于实际环境约束，不能靠提示词豁免。

## 给模型的精简任务示例

“继续维护 Faculty Atlas。完成我指定的用户行为，遵守项目 AGENTS.md。自主处理范围内可逆实现，只对影响结果的缺失信息提问。按改动验证，不增加无关功能。明确区分已实现、已验证和仍需接通的部分。”

## 来源

- 用户本次提供的 AGENTS.md 与七项网站需求。
- 已读网站构建 Skill：/Users/zhichengu/.codex/plugins/cache/openai-curated-remote/sites/0.1.62/skills/sites-building/SKILL.md。
- [OpenAI 官方 GPT-6 Astra 指引](https://developers.openai.com/api/docs/guides/latest-model)。
